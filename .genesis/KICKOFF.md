# KICKOFF — custom-agent-sdk

> Generated from `.genesis/project.json`; do not edit this file.

## Cold-session contract

Load the official Ponytail skill at `full`. Verify repository state and baseline before editing.

- objective: A model-agnostic, LangGraph-pluggable agent SDK with orchestrator-spawned isolated subagents and an evidence-centric governance ledger. Stage 1 delivers the harness (Phases 0-8); Phase 0 is a single-agent loop end to end.
- mode/profile: init / prototype
- workflow: new-product / build
- phase/status: build / active
- active task: M5-postgres — Postgres schema and stores persist runs, messages, run_events and exactly one execution manifest per run, every row tenant-scoped and the trace reconstructable in order
- owner: unassigned
- blocker: none
- next action: Run the task pre-flight.
- source hash: 24fa3bcaae2d1ff47e5519895c9c4df78454e7b2eddbabe9455a8a1137b2f84f
- phase instruction: Implement only the active task and prove it against current sources.
- specification: SPEC.md / approved
- requirements: AC-1, AC-10, AC-2, AC-3, AC-4, AC-5, AC-6, AC-7, AC-8, AC-9, FR-1, FR-10, FR-11, FR-12, FR-13, FR-14, FR-15, FR-16, FR-2, FR-3, FR-4, FR-5, FR-6, FR-7, FR-8, FR-9, NFR-1, NFR-2, NFR-3, NFR-4, NFR-5, NFR-6, NFR-7

## Completed work

- SPEC-1: Produce an approved, implementation-ready product specification · proof: independent-review:pass
- M1-primitives: Canonical primitives, ContentProvenance and the error taxonomy exist as importable types with no I/O · proof: .genesis/evidence/M1-primitives-unit.json
- M2-tool-layer: ToolSpec, ToolRegistry, PermissionChecker, RuntimeHook and the nine-step ToolExecutor lifecycle work, with validation proven to run before permission and permission before execution · proof: .genesis/evidence/M2-tool-layer-unit.json
- M3-model-client: ModelRequest/ModelResponse plus an OpenAI-compatible ModelClient that reaches the configured gateway, with ModelRegistry and the timeout/rate-limit retry policy · proof: .genesis/evidence/M3-model-client-unit.json, independent-review:pass
- M4-loop-and-api: ContextAssembler, AgentLoop and the Runner/AgentSpec/RunConfig/RunResult public surface drive a full tool-using turn against an in-memory session store · proof: .genesis/evidence/M4-loop-and-api-unit.json, independent-review:pass

## Active evidence and failures

- gates: unit:stale (.genesis/evidence/M5-postgres-unit.json), independent-review:pending
- failures: none recorded
- limitations: Carried from M4 review: a tool on the FINAL turn executes with no turn left to consume its result, so a side-effecting tool can fire on a turn whose outcome is max_turns_exceeded. Matches LLD 3.10 literally and is harmless for Phase 0's echo tool, but must be revisited in Phase 2 when tools do more than echo.; Carried from M4 review: RunResult.usage is zeroed when Runner's total boundary catches a non-SDK exception, even though the ModelCalled events still carry per-turn usage. Reconstructible from events but under-reported on the result object; fix while M5 touches Runner for persistence.
- notes: none recorded

## Binding context

- decisions: DECISION-201472de: ADR-13 resolved: OpenAI-compatible wire format, DECISION-ad4f80fb: Phase 0 is the entire spec scope, DECISION-2454cbd8: ADR-27 kept: PrincipalContext as lean metadata, DECISION-442df397: System instructions ride on ModelRequest.instructions, not a fourth Role, DECISION-38ea29c7: Secrets are wrapped in a Secret type, not protected by __repr__ alone, DECISION-f3c125ce: The ModelClient boundary is total, not an enumeration of known exceptions, DECISION-ba428321: Retry belongs to the ModelClient and to exactly one layer, DECISION-ca1ad3e0: tool_profile gates execution, not visibility, in Phase 0, DECISION-03f4e3dd: Manifest is an argument to start_run, not a second write, DECISION-a2c8f342: Provider-supplied numbers are coerced by the type, not by call sites, DECISION-65528763: Canonical primitives are storable by construction, DECISION-aed7e4d8: Message tenancy is derived from the run row, never from the caller, DECISION-53461588: Storability is applied by walking every field, not a list of names, DECISION-e692386f: history() is tenant-scoped on read, not only on write, DECISION-56558d52: Tenancy is enforced on every runs and trace statement
- invariants: INVARIANT-fcf02d03: Every ToolResult carries exactly one ContentProvenance. A tool result that reaches the model or the session store without provenance is a defect, not a default.; INVARIANT-26e9117f: Every persisted row in every table carries non-null tenant_id and project_id. Multi-tenancy is enforced at write time, never assumed at read time.; INVARIANT-3ad4a24d: ToolExecutor's order is fixed: resolve, then validate arguments, then permission check, then execute. A tool whose arguments failed validation must never reach the permission check, and a denied tool must never reach execution.; INVARIANT-e3f0e425: MODEL_API_KEY never enters model context, never enters a persisted row, and never appears in a RunEvent payload. Credentials reach the provider adapter only.; INVARIANT-3e634c67: Application code calls Runner.run() and nothing else. If a test or example imports AgentLoop, ToolExecutor or any other internal collaborator directly, the public API boundary has already leaked.; INVARIANT-8b2269cb: Only AgentSDKError subclasses may escape ModelClient.send(). A provider's JSONDecodeError, AttributeError or httpx.StreamError reaching AgentLoop is a defect: the loop cannot classify what it cannot name. Note httpx.StreamError descends from RuntimeError, not HTTPError, so catching HTTPError alone is insufficient.; INVARIANT-b4c91070: A boundary's error path must not be able to raise. send() wraps any non-AgentSDKError, but str(exc) runs inside that handler: a hostile __str__ that raises, or returns a non-string, escapes the very mechanism built to make the boundary total. Rendering a caught exception goes through _describe_failure, which guards str(exc) explicitly and sits inside a last-resort catch returning a fixed string.; INVARIANT-af776957: Runner.run() is a total boundary: it always returns a RunResult with a terminal status and never lets an exception reach application code. ToolExecutor.execute() is likewise total and always returns an outcome. Configuration errors (unknown model client, invalid RunConfig) are the deliberate exception and raise at the call site, because a caller bug must surface loudly rather than hide in a RunResult nobody inspects. BaseException passes through as control flow.; INVARIANT-3c123c38: Any number that arrives from a model provider, a persisted event payload or a replayed run reaches a typed field only through a total coercion that cannot raise on Exception. Concretely: never call int(), float() or similar directly on such a value; construct the dataclass and let it coerce. Violations have been rejected three times.
- assumptions: ASSUMPTION-bba5b3f6: PostgreSQL 16 is installed at C:/Program Files/PostgreSQL/16 and listening on localhost:5432, confirmed by an open socket. The superuser password is not yet known to this harness, so the Postgres connection string remains unresolved configuration. [open]; ASSUMPTION-53240ce4: Docker Desktop is installed (29.6.2) but its daemon is not running, so container-based Postgres is not an available fallback without starting Docker first. [open]; ASSUMPTION-6c8f0f59: <db_name> exists on localhost:5432, owned by <db_user>, PostgreSQL 16.13. Verified by DDL round trip: CREATE TABLE, insert with gen_random_uuid(), jsonb read, DROP TABLE all succeed; public schema was empty beforehand. gen_random_uuid() is built in on PG13+, so no pgcrypto extension is needed for UUID primary keys. This supersedes the earlier assumption that Postgres credentials were unresolved. [open]; ASSUMPTION-75110765: Two accepted M3 limitations, both reproduced by an independent reviewer and judged non-blocking. (1) _error_text catches only ValueError, so a 429 whose body is undecodable (verified with 6000-deep JSON) is classified ModelError rather than ModelRateLimited and is therefore NOT retried, deviating from FR-15. One-word fix, latent, needs a pathological body. (2) Redaction is asymmetric: a 4xx error body echoing the credential is redacted, but a 2xx body echoing it flows into message.content and reaches model context, contrary to NFR-4 read absolutely. Judged acceptable because error bodies routinely echo request context while success bodies do not, so the asymmetry tracks a real threat-model difference. Both should be closed when the file is next touched. [open]
- knowledge: KNOWLEDGE-4c98ae06: Gateway fronts 308 models across four providers, KNOWLEDGE-dd86e5ae: Genesis gate commands must use Windows path separators, KNOWLEDGE-4da74cc9: genesis checkpoint and index crash on this host via the Store Python alias, KNOWLEDGE-064c6cde: Store Python alias crash is fixed by the venv, no Windows settings change needed, KNOWLEDGE-294f2901: Redaction must precede truncation, and dict.get needs a hashable key, KNOWLEDGE-b36882ac: Reviewer rotation found defects that reviewer effort did not, KNOWLEDGE-5b727de4: M5 must redact the chained __cause__ and traceback, not only the ModelError message, KNOWLEDGE-18154194: Gate gaps hide as quality degradation, not failure, KNOWLEDGE-af3c98a2: Concurrent appends are safe but not available; Phase 2 needs a retry or lock, KNOWLEDGE-35f7ec28: Database tests can prove the database, not the code, KNOWLEDGE-5d09862c: Concurrent append: safety fixed, availability varies, KNOWLEDGE-eaf9f702: Name what you can, then back it with a total check, KNOWLEDGE-f1c4e06e: Name a test after the invariant it pins, KNOWLEDGE-79add1a1: Over-rejection is a defect in the same class as under-rejection, KNOWLEDGE-fd4720a4: Overlapping guards need a test at each layer, KNOWLEDGE-0e7be079: An exemption in the test is the tell, KNOWLEDGE-aa97d748: Two guards for one property hide each other

## Resume

1. Read this file and the active task in `.genesis/project.json`.
2. Obey the phase instruction; specification and planning phases prohibit product implementation.
3. Inspect relevant decisions, graph neighbors, proof, and recent git history only.
4. Run the baseline command when configured.
5. Report `state -> evidence -> blocker -> next action` before editing.
6. End with `genesis checkpoint <repo>`.

